# 쉬운 시장 흐름 v3.4 검증 기록

상태: **정적 소스 감사·15분봉 통합 적용 기록 보존 / 저장소 파일과 TradingView revision 7 바이트 동일성 및 실제 양방향 알림 관찰 대기**

## 검증 대상

- 최종 파일: `easy_market_flow_v3_4.pine`
- 현재 저장소 파일 SHA256(raw bytes): `CD0DA0FF7EB7865D77BD1EC43757852FAABE80B97341E2A5E6D469D9EB6471DD`
- TradingView 저장본: `쉬운 시장 흐름 + 코인 찾기 [v3.4]`, revision 7
- v3.3 보존본 SHA256: `7ECA02C941B90748800F5C3EEE466311A135E51E413261A61539FC44D7111489`
- 현재 v3.4 줄 수: 1,928

## 저장소 보존·해시 감사

- [x] 통합 브랜치의 v3.3/v3.4 Git blob이 로컬 QAR/Pine 기준 커밋 `deecc31`과 동일
- [x] 통합 과정에서 Pine 소스 로직을 수정하지 않음
- [ ] 현재 저장소 v3.4 파일과 TradingView revision 7의 바이트 동일성 재확인

기존 기록에는 v3.4 SHA256이
`4F141C0FF68484E842038861198F381CD17D828EC2D6AB25E2413592CBDF9DD5`로 적혀 있었지만,
현재 보존된 `deecc31` blob과 작업 파일 어느 쪽에서도 그 값을 재현하지 못했다. 아래 실차트
체크는 당시 로그인 세션에서 확인한 이력으로 보존하되, 현재 저장소 파일이 revision 7 저장본과
바이트 단위로 같다는 근거로 사용하지 않는다. TradingView Editor에서 내보낸 소스와 다시
대조하기 전까지 이 항목은 미확인이다.

## 정적 소스 검토

- [x] v3.3 원본을 `easy_market_flow_v3_3.pine`에 바이트 단위로 보존
- [x] `정밀+정합등급`은 정밀 게이트 통과 후 선택적 정합 점수의 최소 등급을 추가 요구
- [x] 동일 시간봉은 현재 차트 상태를 직접 계산하고, 상위 시간봉만 `[1] + lookahead_on` 사용
- [x] 마감봉 후보 게이트와 하위 시간봉 요청 차단 유지
- [x] 하이킨아시·렌코 등 비표준 차트에서 후보·계획선 차단
- [x] 정밀 모드에서 FVG와 OB 계산을 모두 끄면 후보 차단 및 이유 표시
- [x] cooldown 중 후보는 eligibility가 계속 유지될 때만 종료 후 한 번 표시하고, 끊기면 폐기
- [x] 기존 계획 TP1 이벤트 가격을 새 후보가 같은 봉에 시작해도 덮어쓰지 않도록 스냅샷 사용
- [x] 후보 `alertcondition`은 계획선 생성 여부와 무관하게 전체 롱·숏 후보 이벤트를 수신
- [x] `indicator()` 구조, 자동 주문 금지, TP/SL 동일 봉 순서 불명 처리를 유지

## TradingView 실차트 검증

- [x] 당시 작업 소스를 Pine Editor에 붙여넣고 revision 7로 저장한 세션 기록
- [x] 당시 Pine 클라우드 컴파일 오류 0건 및 차트 인스턴스 업데이트 상태 확인
- [x] BINANCE:TAIKOUSDT.P 일반 캔들 15분봉 확인
- [x] 기본 MTF 큰 흐름 4H, 중간 흐름 1H, 짧은 흐름 15m 확인
- [x] 기본 선별 모드 `정밀+정합등급 (추천)` 및 요약표 `오른쪽 위` 확인
- [x] `QAR·ICT Sync`는 왼쪽 위, v3.4는 오른쪽 위로 동시 적용해 표 겹침 제거
- [x] 차트 레이아웃 저장 완료

## 아직 확인하지 않은 항목

- [ ] 새 롱 정밀 후보의 라벨·계획선·후보/계획 알림 조건이 실제 봉에서 함께 발생
- [ ] 새 숏 정밀 후보의 라벨·계획선·후보/계획 알림 조건이 실제 봉에서 함께 발생
- [ ] TradingView 알림 생성 후 실제 알림 전달
- [ ] Bar Replay에서 진행 중 봉과 마감봉을 비교한 실시간 비리페인트 관찰
- [ ] TradingView revision 7 소스를 다시 내보내 현재 저장소 파일과 SHA256 대조

새 롱·숏 후보와 알림 항목은 컴파일 실패가 아니라 희소 신호의 실제 발생·전달을 아직
관찰하지 못한 것이다. 정적 계약과 양방향 대칭은 독립 코드 감사로 확인했지만, 실제
화면·전달을 보기 전에는 완료로 표시하지 않는다. revision 7 바이트 대조는 희소성 문제가
아니라 당시 해시와 현재 저장소 파일의 연결 증거가 남아 있지 않은 별도 미확인 항목이다.

## 검증 환경과 증거

- 검증일: 2026-07-27
- 사용자 세션: TradingView 로그인 계정 `hummingfilm`
- 심볼/시장: BINANCE:TAIKOUSDT.P (USDⓈ-M 무기한)
- 차트: 일반 캔들, 15분
- 기본 MTF: 240분 / 60분 / 15분
- 당시 통합 화면 기록: `artifacts/tradingview-v34-integrated-taikousdt-15m.png`
- 당시 v3.4 단독 화면 기록: `artifacts/tradingview-v34-final-taikousdt-15m.png`

위 두 화면 파일은 당시 세션 기록에 경로만 남았고 현재 저장소와 로컬 `.codex` 산출물에서
찾지 못했다. 따라서 현재 재현 가능한 증거 파일로 간주하지 않는다.
