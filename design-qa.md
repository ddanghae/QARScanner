# Design QA

**비교 대상**

- 기준 이미지: `C:\Users\CC\.codex\generated_images\01a03644-ac68-70f0-adeb-0de4b87daedd\exec-9e23ce40-abd8-481b-b3db-933c206f8a2b.png`
- 구현 화면: `artifacts/qa/mobile-final-375x812.png`
- PC 확인 화면: `artifacts/qa/desktop-results-top-1440x1024.png`
- 전체 비교 이미지: `artifacts/qa/combined-mobile-qa-final.png`
- 강조 후보·탭 확대 비교: `artifacts/qa/focused-priority-tabs-qa.png`

**크기와 상태**

- 기준 이미지: 853×1844px. 아이폰 12 mini용 375px 폭에 맞춰 375×810px로 축소해 비교했다.
- 구현 이미지: 375×812px, CSS 화면 375×812, 화면 배율 1.
- 두 화면 모두 밝은 테마이며, 세 스캐너 검색이 끝나 결과가 보이는 같은 상태다.
- 종목과 개수는 실시간 공개 자료라 기준 이미지의 9개와 구현 화면의 11개가 다르다. 구조 비교에는 영향을 주지 않는다.

**Findings**

- 처리되지 않은 P0/P1/P2 문제 없음.
- 글자: 시스템 한글 글꼴, 굵기, 크기 순서가 기준 이미지와 같은 수준으로 읽힌다. 작은 안내문도 겹치거나 잘리지 않는다.
- 간격과 배치: 머리글 → 한 줄 검색 요약 → 어두운 강조 후보 → 출처 탭 → 줄 목록 → 하단 검색 버튼 순서가 기준 이미지와 같다. 375px에서 가로 넘침은 0px였다.
- 색: 흰 배경, 진한 강조 영역, 보라색 주 버튼, 파랑/초록/빨강 출처색이 기준 의도와 맞는다.
- 이미지와 아이콘: 별도 사진·일러스트가 없는 화면이다. 새로 만든 가짜 이미지나 아이콘은 없다. 설정은 작은 아이콘 대신 더 알아보기 쉬운 `설정` 글자로 표시했다.
- 문구: 각 결과에 스캐너 출처가 보이고, 서로 다른 스캐너 점수를 직접 비교하면 안 된다는 안내를 유지했다.
- 실제 기능: 세 모드 검색, 출처 탭, 상세 열기/닫기, 검색 설정 열기, 설정 화면 이동/복귀를 눌러 확인했다.
- 접근성: 주요 모바일 버튼은 44px 이상이며, 주 검색 버튼은 50px다. 375×812와 1440×1024 모두 겹침과 가로 넘침이 없다.
- 브라우저 오류 기록: 오류와 경고 0건.

**Comparison History**

1. 첫 비교에서 설명 영역과 `스캔 결과` 제목 때문에 강조 후보가 기준보다 아래로 밀렸고, 하단 버튼이 두 줄이라 목록 공간을 많이 차지했다. 어두운 강조 영역 안의 두 버튼도 박스가 추가되어 기준보다 복잡했다.
2. 모바일에서 설명 영역과 중복 제목을 숨기고, 검색 상태를 한 줄로 줄였다. 강조 영역의 버튼은 글자형 동작으로 바꾸고, 하단 검색과 설정을 한 줄로 합쳤다.
3. 수정 뒤 강조 후보의 시작 위치는 화면 위에서 136px, 높이는 약 184px가 되었고 기준 이미지의 주요 비율과 맞았다. 최종 근거는 `combined-mobile-qa-final.png`와 `focused-priority-tabs-qa.png`다.

**Implementation Checklist**

- [x] 아이폰 12 mini 375×812 확인
- [x] PC 1440×1024 확인
- [x] 세 스캐너 실제 검색 완료 확인
- [x] 스캐너별 탭과 출처 표시 확인
- [x] 상세·설정 주요 동작 확인
- [x] 가로 넘침과 브라우저 오류 확인

**Follow-up Polish**

- P3: 실시간 결과에 안전 경고가 여러 개 붙으면 기준 이미지보다 한 줄이 높아질 수 있다. 경고를 숨기지 않는 것이 더 안전하므로 현재 표시를 유지했다.

final result: passed
